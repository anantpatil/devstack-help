diff --git a/lib/heat b/lib/heat
index cedddd2..76c4412 100644
--- a/lib/heat
+++ b/lib/heat
@@ -173,6 +173,7 @@ function configure_heat {
         iniset $HEAT_CONF DEFAULT enable_stack_adopt true
         iniset $HEAT_CONF DEFAULT enable_stack_abandon true
     fi
+    iniset $HEAT_CONF DEFAULT num_engine_workers "$API_WORKERS"

     sudo install -d -o $STACK_USER $HEAT_ENV_DIR $HEAT_TEMPLATES_DIR

diff --git a/lib/neutron-legacy b/lib/neutron-legacy
index 5abe55c..cbee584 100644
--- a/lib/neutron-legacy
+++ b/lib/neutron-legacy
@@ -1024,6 +1024,7 @@ function _configure_neutron_metadata_agent {
     cp $NEUTRON_DIR/etc/metadata_agent.ini $Q_META_CONF_FILE

     iniset $Q_META_CONF_FILE DEFAULT verbose True
+    iniset $Q_META_CONF_FILE DEFAULT metadata_workers $API_WORKERS
     iniset $Q_META_CONF_FILE DEFAULT debug $ENABLE_DEBUG_LOG_LEVEL
     iniset $Q_META_CONF_FILE DEFAULT nova_metadata_ip $Q_META_DATA_IP
     iniset $Q_META_CONF_FILE DEFAULT root_helper "$Q_RR_COMMAND"
diff --git a/lib/nova b/lib/nova
index a6cd651..f0b96ae 100644
--- a/lib/nova
+++ b/lib/nova
@@ -516,6 +516,7 @@ function create_nova_conf {

         configure_auth_token_middleware $NOVA_CONF nova $NOVA_AUTH_CACHE_DIR
     fi
+    iniset $NOVA_CONF conductor workers "$API_WORKERS"

     if is_service_enabled cinder; then
         if is_ssl_enabled_service "cinder" || is_service_enabled tls-proxy; then

